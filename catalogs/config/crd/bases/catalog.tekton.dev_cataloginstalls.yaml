
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: cataloginstalls.catalog.tekton.dev
spec:
  group: catalog.tekton.dev
  names:
    kind: CatalogInstall
    plural: cataloginstalls
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: CatalogInstall is the Schema for the cataloginstalls API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: CatalogInstallSpec defines the desired state of CatalogInstall
          properties:
            catalogRef:
              type: string
            clusterTasks:
              items:
                properties:
                  name:
                    type: string
                  version:
                    type: string
                required:
                - name
                - version
                type: object
              type: array
            tasks:
              items:
                properties:
                  name:
                    type: string
                  version:
                    type: string
                required:
                - name
                - version
                type: object
              type: array
          required:
          - catalogRef
          - clusterTasks
          - tasks
          type: object
        status:
          description: CatalogInstallStatus defines the observed state of CatalogInstall
          type: object
      type: object
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
